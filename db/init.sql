CREATE TABLE Cash (
    id Serial PRIMARY KEY,
    dolar varchar
);

CREATE TABLE Weather (
    id Serial PRIMARY KEY,
    temp varchar
);

CREATE TABLE Uf (
    id Serial PRIMARY KEY,
    uf varchar
);